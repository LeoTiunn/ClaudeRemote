name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup debug keystore
        run: |
          mkdir -p ~/.android
          echo "${{ secrets.DEBUG_KEYSTORE }}" | base64 -d > ~/.android/debug.keystore
          # Convert PKCS12 to JKS for Android
          keytool -importkeystore \
            -srckeystore ~/.android/debug.keystore \
            -srcstoretype PKCS12 \
            -srcstorepass android \
            -destkeystore ~/.android/debug-android.keystore \
            -deststoretype JKS \
            -deststorepass android \
            -destkeypass android \
            -srcalias androiddebugkey \
            -destalias androiddebugkey 2>/dev/null || true
          # Use JKS if conversion worked, otherwise keep PKCS12
          if [ -f ~/.android/debug-android.keystore ]; then
            mv ~/.android/debug-android.keystore ~/.android/debug.keystore
          fi

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Run Unit Tests
        run: ./gradlew testDebugUnitTest

      - name: Upload Debug APK
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: app/build/outputs/apk/debug/*.apk

      - name: Upload APK to latest release
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LATEST_TAG=$(gh release list --limit 1 --json tagName -q '.[0].tagName')
          if [ -n "$LATEST_TAG" ]; then
            APK_PATH=$(ls app/build/outputs/apk/debug/*.apk | head -1)
            gh release upload "$LATEST_TAG" "$APK_PATH" --clobber
          fi
